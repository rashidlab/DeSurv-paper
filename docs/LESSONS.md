# LESSONS.md

One row per **recurring mistake class**, each naming the **check that would have caught it**.
Not a bug log: a bug goes here only when it has happened twice, or when it cost a day.

Rule (from the `paper` skill): *a row is not finished until its check is written and proven to
fire by perturbing the input.* Where the check is still "manual", say so honestly rather than
implying automation that does not exist.

This file is **tracked in git**. `docs/` is otherwise gitignored, so this file is force-added
(`git add -f docs/LESSONS.md`). That is deliberate: the whole point is that it survives.

---

| # | Mistake class | What happened | Check that catches it | Status |
|---|---|---|---|---|
| L1 | **Undefined technical term at first use** | "rank", "reconstruction", "Bayesian optimization", "one-standard-error rule", "Shapley share", "consensus initialization", "latent", "linear predictor", "cutpoint" all appeared in the main text with no definition. The PI and I agreed verbally that terms must be defined at first use; the rule was never written into the plan, into memory, or into any checklist, so **nothing carried it forward**. Recurred 2026-08-06, when a sentence I wrote that day introduced "consensus initialization" undefined. Note also that "rank" and "reconstruction" were first defined at their first *Results* use, while their true first use is in the Abstract/Introduction. | **`make check-terms`** (`code/util_check_first_use.R`). Regex cannot judge whether prose defines a term, and a cue-word version produced false flags both ways, so the judgement is recorded once by a human and the machine enforces it: each term maps to the exact definitional phrase required near first use, and terms deliberately left undefined are listed with a reason. Fires if a definition is edited away. | **check written, proven to fire** (removed the "reconstruction error" definition → exit 1; restored → exit 0) |
| L2 | **Trusting an object's name instead of the code that produced it** | The Fig 5 caption reported `treated_cohort_stats$meta` as a D1 statistic. It is built from `d2adj` in `code/11:157-165`, i.e. **D2, PurIST+DeCAF-adjusted**, and `code/11:19-22` says it is not for manuscript use. This turned a null result (D1 pooled P=0.30) into a significant one (P=0.005) in a main figure. One auditor verified the number matched the object and passed it; only tracing provenance into the script caught it. | Before quoting any cached value, open the script that writes it and read the assignment, not just the object name. **Correction 2026-09-05:** this row previously named `scripts/check_result_provenance.R` as a written check; **that file does not exist in the tree** and never did in any commit reachable from this branch. The field-level provenance check is therefore **manual**. `scripts/check_build_graph.sh` (L12) covers only the coarser object-to-script mapping; a field-level script remains to be written. | **manual; earlier "check written" was false** |
| L3 | **Verification method that fails silently** | Two so far. (a) `grep "undefined reference" paper/*.log` always passes because `code/10_render_paper.R:54-56` **deletes** the logs. (b) Word counters that skip bare numerals under-count; and on 2026-08-06 a span-detection counter silently reported 2,411 instead of 4,081 because an edit added the word "Methods", shifting its index. | Every check must be **proven to fire**: perturb the input and confirm it fails. Reference checks use `pdftotext paper/paper.pdf - \| grep -c '??'` (0 = clean). Word counts anchor on `\nMethods\n` as a heading and are sanity-checked against page count. | adopted |
| L4 | **A destructive default assumed to be safe** | `code/00_helpers.R:18` sets `recompute = !identical(Sys.getenv("DESURV_RECOMPUTE"), "FALSE")`, so running any `code/NN_*.R` plainly **recomputes and overwrites** cached results, including the BO that anchors every published number. CLAUDE.md documented the opposite. Nearly destroyed the canonical cache on 2026-08-06. | Long runs go through `slurm/run_fair_comparator.sh`, whose preflight puts `stop("CACHE BYPASSED")` **inside** `cache_or_compute` for each canonical object and aborts if it fires. Never launch a multi-hour job without a preflight that proves the cache is being read. | check written, fired correctly |
| L5 | **Wrong dependency version silently substituted** | `code/01_install.R` prefers a local `../DeSurv` checkout, which on this machine is Amber's fork at a commit that does not contain the pinned `afb00d5`. Installing it would have fit a new comparator with different optimizer code than produced the cached fits, reintroducing the exact confound the analysis existed to remove. | The `run_fair_comparator.sh` preflight asserts `packageVersion("DeSurv") == "1.0.1"`. Install explicitly with `remotes::install_github("rashidlab/DeSurv@submission/pnas-2026")`, never `make install`. | check written |
| L6 | **Word budget consumed without funding the cut** | Additions were made to a main text already at 3,998/4,000 without first naming the cut that pays for them, requiring several rounds of retroactive trimming. | Before adding main-text prose, name the cut. Numbers and definitions belong in **figure legends, table captions and Methods**, which are excluded from the cap. `docs/concision-cut-list.md` holds the pre-identified reserve. | adopted |
| L8 | **A prose-tightening pass deletes definitions** | 2026-08-15, during the line-by-line pass. Two of the seven checks in that method (sentence construction, repetition) delete words for a living, and definitions are made of words. Unwinding the nominalization "together **form a representation of** the tumor transcriptome" to "together **represent**" is better prose and destroyed the first-use definition of "representation"; repairing a garden path by rewriting "simulations in which **the true underlying (latent) programs are known**" to "simulations with known latent programs" destroyed the definition of "latent". **Both regressions came from correct applications of the method**, which is why the writer cannot self-catch them. | **Re-render, then run `make check-terms`, after every applied batch**, not once at the end. The order is not optional: the check reads the rendered PDF, so running it before `make paper` returns a stale verdict and can falsely pass a definition you just deleted (**see L9**). Where a construction fix and a definition collide, the definition wins: the `latent` repair kept the garden-path fix by adding a comma ("...are known**,** to characterize...") rather than deleting the parenthetical, since the intervening words were never the real problem. | **fired correctly on both** (caught them at the time; restored, re-verified) |
| L9 | **A checker that reads a build artifact, run before the build** | `make check-terms` reads `paper/paper.pdf`, not the `.Rmd` sources. Run immediately after editing and before `make paper`, it judged text that no longer existed and reported a **false FAIL** on definitions already restored. The dangerous direction is the converse: edit a definition away, run `check-terms` without re-rendering, and get a **false PASS**. | **`make check-terms` must run after `make paper`, never before.** Same family as L3: the check is only as fresh as the artifact it reads. Any check consuming a rendered PDF inherits this constraint. | adopted |
| L10 | **`pdftotext` layout artifacts defeat fixed-string matching** | 2026-08-15: a prose edit moved the "gene program" definition across a page boundary. `pdftotext` emits the page number as its own line and glues a form feed to the first word of the next page, so the running text read `"...gene programs: weighted" / "1" / "" / "\fsets of co-varying genes..."`. `make check-terms` reported a FAIL for a definition that was present and correctly placed. **A false FAIL is not benign here**: the script's own message invites the reader to edit `DEFINED` instead, which would silently retire the check. Same root cause as the earlier incident where `grep` on `pdftotext` output returned zero hits for edits that had in fact landed. | `code/util_check_first_use.R` now strips form feeds **first**, then standalone page-number lines, then collapses the resulting double spaces. Order matters, because the form feed can be glued to the page number itself. Generally: **never fixed-string match raw `pdftotext` output**; normalize page furniture and whitespace first, and treat a zero-hit grep on PDF text as unproven rather than as absence. | **check written, proven both directions** (page-straddling definition → PASS; definition genuinely deleted → exit 1 for `gene program` and `representation`) |
| L11 | **A reference check cannot detect a reference converted into plain text** | 2026-09-05: a Python `re.sub` replacement string containing `\\ref` emitted a carriage return plus "ef", so the manuscript printed "Supplementary Figs. ef{fig:...}" while the unresolved-reference check passed, because no `\ref` remained to be unresolved. The defect is invisible to every check that looks for `??`. | After any regex-driven edit of manuscript sources: `grep -c $'\r' paper/*.Rmd` must be 0, and the rendered text is grepped for the literal `ef{`. Count `\ref{...}` occurrences before and after an edit batch; a net loss is a finding. | **check written** (`scripts/check_prose_integrity.sh`), **proven to fire** (injected CR → exit 1; restored → exit 0) |
| L12 | **A green render does not establish clean-build reproducibility** | 2026-09-05: the manuscript read five cached objects (`code/20`, `21`, `23`, `24`, `25` outputs) that `make all` never produced and `make clean` deleted. Every render was green only because of residual cache state; `make clean && make all && make paper` would have failed at render. Two of the five predated the day's work. | The invariant for a submission repository is `make clean && make all && make paper` succeeding from the repository alone. Every `read_result()` / `load_result()` name in `paper/*.Rmd` must map to a script in the `all` target; `scripts/check_build_graph.sh` maps each read to its writer and fails if that writer is absent from `make -n all`. Prove wiring by regenerating the objects in a scratch copy and comparing with `all.equal()` plus checksums. | **check written** (`scripts/check_build_graph.sh`), **proven to fire** (removed `code/25` from `all` → exit 1; restored → exit 0); regeneration proof in a scratch worktree recorded in the commit message |
| L13 | **Quoting a function's signature defaults instead of the values the call path actually passes** | 2026-09-06: the SI's algorithm description was corrected against `src/functions.cpp`, whose `update_W_damped_backtrack` declares defaults `rho = 0.9, max_backtracks = 30`. The production fits reach it through `R/optimizer_wrappers.R`, which passes `rho = 0.5, max_backtracks = 10`; `desurv_fit()` does not override them. The committed SI therefore reported constants no reported fit ever used. Same family as L2 (the defining site was trusted instead of the invocation). Caught by post-commit review, not by any guard. | For any submission-critical description of an algorithm, trace every constant from the entry point the pipeline calls (`desurv_fit` here) through each wrapper to the routine that uses it, and report the **invoked** value; a signature default is evidence only when no caller overrides it, which must be shown by reading the callers, not assumed. Record the call path beside the value (the `code/26` header does). | **manual** (no automated check; the rule is applied by reading the call chain) |
| L7 | **A `seed` argument that does not make the result reproducible** | `R/cv_grid_helpers.R:135-152` passes `seed` to `DeSurv::desurv_fit()` but also `parallel_init = TRUE, ncores_init = 30`; the forked initialization workers do not inherit a reproducible RNG stream. Four identical calls selected z-cutpoints of 1.00, 0.80, 1.20 and 2.00, moving the validation high-risk fraction from 5% to 33%. The value was cached, so the manuscript was self-consistent and nothing looked wrong; it surfaced in Aug 2026 only because a comparator was recomputed. The dichotomized-risk-group result (main-text Fig. 3b and one SI section) was **removed** rather than re-derived, since no reported claim depended on it. | A seed is not evidence of reproducibility. For anything whose value reaches the manuscript, **run it twice and diff** before reporting. Parallel RNG needs `RNGkind("L'Ecuyer-CMRG")`; absent that, treat any `parallel_*` fit as unseeded. Cached `.rds` outputs hide this class of defect entirely. | check written |

---

## How to use this file

- **Before a writing pass**: read the table. Two minutes.
- **Before submission**: run every check in the Check column.
- **When something breaks twice**: add a row. Second occurrence is the promotion rule.
- **When a review (codex, LLM, human) catches something**: if it is an instance of an existing
  row, note it there; if it is new *and* recurring, add a row. One-off typos do not belong here.

## Known gap

Review findings currently accumulate in `docs/manuscript-review-findings.md`, which is a
per-pass working document **and is gitignored**, so nothing survives the pass. Findings that
represent a recurring class should be promoted into this table, which is tracked.
